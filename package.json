{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.4.0"
  },
  "scripts": {
    "dev": "lerna run dev --parallel --scope \"@bmd/{cms-starfish,apa}\"",
    "test": "CI=true npm test --prefix packages/components"
  },
  "workspaces": [
    "packages/*"
  ],
  "packages": [
    "packages/*"
  ],
  "npmClient": "yarn",
  "useWorkspaces": true
}
