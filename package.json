{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.4.0"
  },
  "scripts": {
    "dev": "lerna run dev --parallel --scope \"@bmd/{cms-starfish,apa}\""
  },
  "workspaces": [
    "packages/*"
  ]
}
